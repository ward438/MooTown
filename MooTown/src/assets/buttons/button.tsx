// Button.tsx
import React from 'react';
import { otherButton } from '../../styles/styles.css.ts';

const Button: React.FC = () => {
  return <button className={otherButton}>Click Me</button>;
};

export default Button;
